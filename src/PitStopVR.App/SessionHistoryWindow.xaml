<Window x:Class="PitStopVR.App.SessionHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Historial de sesiones" Height="500" Width="780" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Historial de sesiones" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Juego:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <ComboBox x:Name="GameFilterComboBox" Width="220" SelectionChanged="GameFilterComboBox_SelectionChanged"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1" x:Name="SessionsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,12" SelectionChanged="SessionsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fecha" Binding="{Binding StartedAt}" Width="140"/>
                <DataGridTextColumn Header="Juego" Binding="{Binding GameName}" Width="160"/>
                <DataGridTextColumn Header="Perfil" Binding="{Binding ProfileName}" Width="120"/>
                <DataGridTextColumn Header="Modo" Binding="{Binding IsSimulated}" Width="80"/>
                <DataGridTextColumn Header="FPS prom." Binding="{Binding AvgFps}" Width="80"/>
                <DataGridTextColumn Header="Cuello de botella" Binding="{Binding Bottleneck.Title}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="RefreshButton" Content="Actualizar" Padding="12,6" Click="RefreshButton_Click" Margin="0,0,8,0"/>
            <Button x:Name="ExportButton" Content="Exportar" Padding="12,6" Click="ExportButton_Click" IsEnabled="False" Margin="0,0,8,0"/>
            <Button x:Name="DeleteButton" Content="Eliminar" Padding="12,6" Click="DeleteButton_Click" IsEnabled="False" Background="Red" Foreground="White" Margin="0,0,8,0"/>
            <Button x:Name="ViewDetailButton" Content="Ver detalle" Padding="12,6" Click="ViewDetailButton_Click" IsEnabled="False" Background="Green" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
